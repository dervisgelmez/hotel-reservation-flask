
"""
con = mysql.connector.connect(user='root', password='root',
                           host='127.0.0.1',
                           database='cinar')
c = con.cursor()
  users = []
    c.execute("SELECT * FROM client")
    for row in c:
        user = {
            'id': row[0],
            'first_name': row[1],
            'last_name': row[2],
        }
        users.append(user)
"""
